<h1>All Tags</h1>
<ul>
  <% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag), class: 'article_title', id: "tag_#{tag.id}" %> | <%= link_to "Delete Tag", tag_path(tag), method: :delete, data: {confirm: "Are you sure?"} %></li>
  <% end %>
</ul>